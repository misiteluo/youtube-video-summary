# 复制为 .env 并填写真实值

# Google Gemini API Key（必填，用于总结）
# 在 https://aistudio.google.com/apikey 申请
GEMINI_API_KEY=your_gemini_api_key

# 可选：指定模型，默认 gemini-2.5-flash（也可用 gemini-1.5-pro）
# GEMINI_MODEL=gemini-2.5-flash

# 收件邮箱（也可在命令行用 --to-email 指定）
TO_EMAIL=your@email.com

# 发件邮箱 SMTP（必填，若需要发邮件）
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your_sender@qq.com
SMTP_PASSWORD=你的授权码
